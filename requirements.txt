# ─────────────────────────────────────────────────────────────────────────────
# RAG System — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic>=2.10
pydantic-settings>=2.6
python-dotenv>=1.0

# ── LLM Providers (choose one in .env) ───────────────────────────────────────
openai>=1.50                     # OpenAI API (GPT-4o-mini)
google-generativeai>=0.8         # Google Gemini API
httpx>=0.27                      # Ollama REST client

# ── Embeddings ───────────────────────────────────────────────────────────────
sentence-transformers>=3.0       # Local embeddings (all-MiniLM-L6-v2)

# ── Vector Store ─────────────────────────────────────────────────────────────
chromadb>=0.5                    # Persistent local vector database

# ── Document Parsing ─────────────────────────────────────────────────────────
pypdf>=5.0                       # PDF text extraction

# ── Text Splitting ───────────────────────────────────────────────────────────
langchain-text-splitters>=0.3    # RecursiveCharacterTextSplitter
